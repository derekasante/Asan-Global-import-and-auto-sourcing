# TODO_AI_WIDGET_REFACTOR

- [x] Refactor AI assistant widget: move inline CSS/JS out of `index.html` into a dedicated stylesheet and script (or `style.css` + `index.js`).

- [ ] Replace inline handlers (`onclick`, `onkeypress`) with `addEventListener` in JS.
- [ ] Ensure z-index/positioning doesn’t conflict with fixed header and floating WhatsApp.
- [ ] Add smooth open/close transitions and improve mobile sizing.
- [ ] Improve anchor scrolling with fixed header offset (`scroll-margin-top` or equivalent).
- [ ] Quick manual verification steps.

