/**
 * Asan Global — Frontend Supabase configuration
 * ---------------------------------------------------------------
 * SECURITY NOTE:
 *   This file contains ONLY the PUBLIC Supabase URL and the ANON key.
 *   The anon key is designed to be safe for client-side usage.
 *   The SUPABASE_SERVICE_ROLE_KEY MUST stay server-side (in .env)
 *   and must NEVER be placed in any frontend file.
 *
 *   This file is generated from .env (SUPABASE_URL + SUPABASE_ANON_KEY).
 */
window.ASAN_SUPABASE = {
  url: "https://zcovmqiuixqrnkgmwebq.supabase.co",
  anonKey: "sb_publishable_h8rPzKH68M1RStJiuU2d4g_opbFzSgy"
};
