# TODO_PRO_STYLE

## Phase 1 — Unify “pro” look
- [x] Verify marketing page `index.html` uses `style.css` and already looks pro
- [x] Fix `index 2.html` to remove missing `dashboard.css` and use shared `style.css`
- [x] Fix `dashboard2.html` to use shared `style.css` (already was)

## Phase 2 — Next pages (planned)
- [ ] Identify which `index5.html`–`index28.html` should be updated
- [ ] Update their `<link rel="stylesheet">` to use `style.css`
- [ ] Ensure dashboard shell styles exist for pages that use `.sidebar`, `.main`, `.cards`, etc.
- [ ] Sanity check mobile responsiveness

